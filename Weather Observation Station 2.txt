select convert(decimal(10,2),sum(LAT_N)) as lat ,convert(decimal(10,2),sum(LONG_W)) as lon from STATION
--convert the number to decimal with 2 float digit after sum
--select round(sum(LAT_N),2) as lat ,round(sum(LONG_W),2) as lon from STATION work well on mysql